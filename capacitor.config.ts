import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.kaanlikescoding.fitness",
  appName: "Fitness",
  webDir: "dist",
  bundledWebRuntime: false
};

export default config;
